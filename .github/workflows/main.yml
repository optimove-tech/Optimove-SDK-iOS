name: CI
on: [push]
jobs:
  unit-tests:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v1
      - run: make test
        name: Test
        env:
          # CI must use the pinned Xcode, never silently fall back to another one.
          XCODE_STRICT: 1
